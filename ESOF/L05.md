# Lab Cypress:

```
Testes End-to-End

Cypress: Testes completos da aplicação
- Simular comportamento do cliente com interação no browser

>> describe(
>> it(
>> Antes de todos os testes: after(
>> Antes de cada teste: beforeEach( 
>> o mesmo para $  after( e afterEach(   $ 

Como correr:
npx cypressrun

```
